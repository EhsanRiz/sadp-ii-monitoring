# Copy to .env (same folder) and fill in real values from Supabase Dashboard
# → Project Settings → API
#
# Use the SERVICE ROLE key for catalog seeding — it bypasses RLS.
# NEVER commit .env to git. NEVER use service role key in the frontend.

SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
